// Admin no tiene tabla propia.
// Este archivo puede exportar tipos auxiliares del panel de administración.

export interface FiltrosUsuario {
  rol?: string;
  pagina?: number;
  limite?: number;
}   